{
  "name": "chord-progression-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd app && npm run dev",
    "build": "cd app && npm run build",
    "preview": "cd app && npm run preview",
    "validate-chords": "cd app && npx tsx validate-chords.ts"
  }
}
